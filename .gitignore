# The fetched binaries — installed locally, not redistributed via the repo.
bin/wmctrl.exe
bin/xdotool.exe

# Temporary screenshot output if anyone runs the recipes from inside the repo.
*.png
!references/**/*.png

# Windows OS noise
Thumbs.db
Desktop.ini
ehthumbs.db
ehthumbs_vista.db
$RECYCLE.BIN/
*.lnk

# PowerShell ISE / VSCode workspace
*.psproj
*.code-workspace
.vscode/

# JetBrains (in case anyone opens this in Rider for the install script)
.idea/

# Build artifacts (in case someone rebuilds the C# from upstream source here)
*.pdb
*.obj
*.exe
!bin/.gitkeep

# Logs / scratch
*.log
*.tmp
.scratch/

# macOS noise (in case someone clones on a Mac to inspect)
.DS_Store

# Skill workspace files generated by skill-creator-style evals (if anyone
# adds tests later).
**/iteration-*/
**/*-workspace/
feedback.json
